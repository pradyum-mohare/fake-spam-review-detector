# fake-spam-review-detector
This project is an NLP-based machine learning application that detects whether an e-commerce product review is fake or genuine using TF-IDF and a Linear SVM classifier. A Streamlit web app is used to provide real-time prediction from user input.
